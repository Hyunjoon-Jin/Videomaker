# 「땅과 때」 작업 지침

## 보고는 열 줄 이내

채팅으로 하는 보고는 **전부 합해 열 줄을 넘기지 않는다.** 표·목록·본문
다 합쳐서다.

무엇을 했고, 무엇이 나왔고, 다음이 무엇인지만 적는다. 근거·시행착오·
판단의 이유는 코드 주석과 `docs/`에 남긴다. 채팅에 옮겨 적지 않는다.

길게 써야 할 것 같으면 그건 문서에 들어갈 내용이다.

예외는 둘뿐이다.

1. **결정을 요청할 때** — 선택지와 각각의 결과는 다 적는다. 판단에
   필요한 것을 빼면 답을 할 수 없다.
2. **제목·설명·고정댓글 원문** — 그대로 복사해 쓸 것이라 줄여 쓸 수 없다.

## 문체

**~다 체를 쓰지 않는다.** 자막·제목·화면 문구 전부 명사구로 닫는다.
'…했다', '…이다', '…한다' 금지. 코드 주석과 문서는 예외다.

## 편을 고르는 기준 — 넷 다 만족해야 만든다

1. **야마가 하나 있어야 한다.** 고유하고 명확한 주제 하나. 배치도나
   목록은 야마가 아니다. "무엇을 보여주는가"가 아니라 **"무엇을
   주장하는가"**로 한 줄이 써져야 하고, 편 전체가 그 한 줄을 증명하는
   데만 쓰인다.
2. **60초 이내.**
3. **BGM은 주제에 어울리는 무료 이용 가능 음원.** 분위기만 맞추지 말고
   소재와 맞물리게 고른다.
4. **누구나 아는 사실이면 안 된다.** 사람들이 모르는 사실이어야 하고,
   흥미를 끌어야 한다. 이건 둘로 갈라진다.
   - **딱 봐도 클릭하고 싶은 제목**이 나오는가
   - **썸네일로 쓸 장면**이 편 중간에 있는가

   기획 단계에서 제목과 썸네일 장면을 먼저 적어본다. 안 나오면 그
   소재는 접는다. 다 만들고 나서 제목을 짜내는 순서면 늦다.

## 편을 만드는 순서

1. **데이터부터 확보한다.** 설계 전에 받아본다. 못 받으면 접는다
   (영해 편, 간척 편이 그래서 보류됐다).
2. **가설을 먼저 검증한다.** 편의 전제가 실제 데이터로 성립하는지
   설계 전에 잰다.
3. 그 다음에 화면을 짠다.

## 넘으면 안 되는 선

- **쇼츠는 59초 이하.** 1분 넘는 쇼츠는 Content ID 소유권 주장이 살아
  있는 동안 차단된다. `python3 scripts/check-lengths.py`로 검사한다.
- **BGM은 공유마당에서** (`scripts/find-bgm.py`). Kevin MacLeod 음원은
  본인이 Content ID에 등록해 둬서 올릴 때마다 클레임이 걸린다.
  CC BY는 설명란 표기가 이용 조건이라 빼면 라이선스 위반이다.
- **자막을 고치면 길이가 바뀐다.** BGM을 다시 자르고 길이를 다시 잰다.
- **화면 고지에 '고정댓글'을 언급하지 않는다.** 무엇이 기록이고 무엇이
  근사인지만 적는다.

## 화면

- **0프레임이 완성된 그림이어야 한다.** 지도는 처음부터 떠 있고 훅
  글자는 타이핑 없이 다 보인다. 훅은 2.2초.
- 위 352px·아래 240px·오른쪽 기둥(930~1080, y 1130~1760)은 쇼츠 UI가
  덮는다. `src/safe.ts`가 기준이고 `scripts/safe.py`로 검사한다.
- **기록값과 계산값을 구분해 적는다.** 그리고 순위나 '나머지'를 말하는
  순간, 그 목록이 닫힌 집합인지 따로 확인한다. 값 하나하나가 맞는 것과
  목록이 빠짐없는 것은 다른 검증이다.

## 1~7편

다시 묻지 않는다. 끝난 이야기다.
